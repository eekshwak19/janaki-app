services:
  - type: web
    name: janaki-app
    runtime: node
    buildCommand: npm run install:all && npm run build:client
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
